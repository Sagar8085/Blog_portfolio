const express = require("express");

const router = express.Router();

router.post('addproject',projectController.addProject)
router.get('projects',projectController.addProject)
router.put('project',projectController.addProject)
router.delete('project',projectController.addProject)
